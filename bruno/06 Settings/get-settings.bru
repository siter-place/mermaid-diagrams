meta {
  name: Get settings
  type: http
  seq: 1
}

get {
  url: {{apiRoot}}/settings
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.values: isDefined
  res.body.schema: isDefined
}
