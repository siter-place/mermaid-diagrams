meta {
  name: Get diagram usage
  type: http
  seq: 1
}

get {
  url: {{apiRoot}}/diagrams/{{diagramId}}/usage
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.usageCount: isNumber
}
